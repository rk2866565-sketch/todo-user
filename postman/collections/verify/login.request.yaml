$kind: http-request
name: login
url: http://localhost:9002/user/login
method: POST
body:
  type: json
  content: "{\r

    \   \"email\": \"rohitlogin+22@gmail.com\",\r

    \   \"password\": \"123456\"\r

    }"
scripts:
  - type: afterResponse
    code: "var jsonData = pm.response.json();\r

      console.log(jsonData)\r

      if (jsonData.accessToken) {\r

      pm.environment.set(\"accessToken\", jsonData.accessToken);\r

      }\r

      if (jsonData.refreshToken) {\r

      pm.environment.set(\"refreshToken\", jsonData.refreshToken);\r

      }"
    language: text/javascript
order: 3160176740046604
